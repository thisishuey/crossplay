fastapi==0.115.6
uvicorn==0.32.1
# The daily-schedule mode resolves a local time in a named zone, and a slim
# image has no zone database of its own.
tzdata==2024.2
# Tests only: fastapi's TestClient is built on it, and tests/test_live_events.py
# drives the pairing and check-in paths through the real app. Pinned in
# requirements.txt rather than a second file because that is what
# study-bridge does and check.sh builds every bridge's venv the same way.
httpx==0.28.1
